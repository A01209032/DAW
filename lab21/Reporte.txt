�Qu� hace el primer bloque del c�digo (bloque del IF)? 
	Checa si el procedimiento ya existe, y si si lo borras

�Para qu� sirve la instrucci�n GO? 
	Para que el programa no interprete el script despues del go hasta que termine lo que este arriba del go

�Explica que recibe como par�metro este Procedimiento y qu� tabla modifica?
	Recibe de parametro los valores del nuevo material que se va a agregar a la tabla Materiales,modifica la tabla Materiales

Explica en tu reporte qu� recibe como par�metro este procedimiento y qu� hace.
	Recibe como paramentro lo que va a buscar en el query, y ejecuta la query

�Qu� ventajas tienen el utilizar Stored Procedures en una aplicaci�n cliente-servidor? 
	Facilita la interaccion con la base de datos

�Qu� ventajas tiene utilizar SP en un proyecto?
	Tiene de ventaja que puedes dividir las cosas que tiene que hacer el equipo de desarollo 

Stored procedure para lab 16: se ejecuta en util.php

  delimiter //
  CREATE PROCEDURE creaCuenta(
                username varchar(50),
                email varchar(50),
                password varchar(50))
            BEGIN
               INSERT INTO `usuarios`(`username`, `email`, `password`) values(username,email,password);
            END


Stored procedure para el proyecto:

  delimiter //
  CREATE PROCEDURE creaServicio(
                idD int(25),
                idP int(25),
                idV int(25),
                fecha date,
                idS int(25),
                Observaciones varchar(140),
                CR float)
            BEGIN
               INSERT INTO `atienden`(`IdDepartamento`, `IdPaciente`, `IdVoluntario`, `Fecha`, `IdServicio`, `Observaciones`, `CuotaRecup`) values(idD,idP,idV,fecha,idS,Observaciones,CR);
            END//


